
<div class="alert alert-danger display-none" id="error-messages"></div>

{{ csrfField }}

<div class="form-group">
  {{ form.label('name', 'Enter name', { class: 'col-md-12' }) }}
  <div class="col-md-6">
    {{ form.text('name', "", { class: 'form-control' }) }}
  </div>
</div>

<div class="form-group">
  {{ form.label('icon', 'Image', { class: 'col-md-12' }) }}
  <div class="col-md-6">
    <select class="form-control" name="icon" id="icon">
      <option selected>Select Image</option>
      {% for image in files %}
        <option value={{image.name}}>{{image.name}}</option>
      {% endfor %}
    </select>
  </div>
</div>
